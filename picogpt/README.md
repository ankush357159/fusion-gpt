1. Create virtual environment `python -m venv .venv`
2. Activate virtual environment `.\.venv\Scripts\activate`
3. Initialize project `uv init`
4. Add pacakges `uv add torch numpy transformers datasets`